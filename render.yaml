services:
  - type: web
    name: flask-convocatorias
    env: python
    rootDirectory: flask_full_project
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.12

